# Frontend

This directory is the full application.

- UI pages live in `src/app`
- Internal API routes live in `src/app/api`
- Shared client helpers live in `src/lib`
- Shared server helpers live in `src/lib/server`

Run locally:

```bash
npm install
npm run dev
```
